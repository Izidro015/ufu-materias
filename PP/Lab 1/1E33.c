#include <stdio.h>

int main ()
{
    int L;
    int A;
    printf("digite o tamanho de uma lado do quadrado:");
    scanf("%d", &L);
    A=L*L;
    printf("a área desse quadrado é: %d", A);
    return 0;
}